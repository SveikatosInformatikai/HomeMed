<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homemed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MeniuActivity"></activity>
        <activity android:name=".UserProfile" />
        <activity android:name=".Duomenu_isvedimasActivity" />
        <activity android:name=".SutrikimaiActivity" />
        <activity android:name=".Skausmo_ivedimas" />
        <activity android:name=".Simptomu_ivedimas" />
        <activity
            android:name=".simtomu_irasymas"
            android:label="@string/title_activity_simtomu_irasymas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Atsiprasymo_langas_kai_simptomu_nera" />
        <activity android:name=".simptomu_suvedimas" />
        <activity android:name=".activity_ar_yra_simptomu" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>